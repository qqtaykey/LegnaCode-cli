/**
 * Register bundled workflow definitions.
 * Called at module load time in tools.ts before WorkflowTool is instantiated.
 * Currently a no-op — bundled workflows will be added here as they are created.
 */
export function initBundledWorkflows(): void {
  // No bundled workflows registered yet.
}
